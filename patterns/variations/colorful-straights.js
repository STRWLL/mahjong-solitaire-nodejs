import { MNColorfulStraights } from "../prototypes/m-n-colorful-straights.js";

export class ColorfulStraights extends MNColorfulStraights {
  constructor() {
    super(
      '三色同順',
      3,
      1,
      2,
      0,
    );
  }
}
